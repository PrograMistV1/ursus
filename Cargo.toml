[workspace]
members = [
    "ursus-core",
    "engine-gltf-loader",
    "engine-obj-loader",
    "ursus-pipelines",
    "engine-macros",
    "ursus-demo",
    "engine-materials",
]
resolver = "2"

[profile.dev.package."*"]
opt-level = 3
