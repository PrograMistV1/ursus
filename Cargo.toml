[workspace]
members = [
    "engine-core",
    "engine-gltf-loader",
    "engine-obj-loader",
    "engine-pipelines",
    "engine-macros",
    "engine-demo",
]
resolver = "2"

[profile.dev.package."*"]
opt-level = 3