[workspace]
members = [
    "ursus-core",
    "engine-gltf-loader",
    "engine-obj-loader",
    "engine-pipelines",
    "engine-macros",
    "engine-demo", "engine-materials",
]
resolver = "2"

[profile.dev.package."*"]
opt-level = 3
